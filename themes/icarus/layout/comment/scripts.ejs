<% if (theme.comment.disqus) { %>
    <%- partial('comment/disqus', { script: true }) %>
<% } else if (theme.comment.gentie) { %>
    <%- partial('comment/gentie', { script: true }) %>
<% } else if (theme.comment.youyan) { %>
    <%- partial('comment/youyan', { script: true }) %>
<% } %>
